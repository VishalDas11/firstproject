import 'package:flutter/material.dart';

class AppColor {
  // Primary colors (used for backgrounds, headers, etc.)
  static const Color primary = Color(0xffc6e2f7); // Light blue
  static const Color primaryDark = Color(0xff2196f3); // Dark blue (alternative)
  static const Color secondary = Color(0xffffffff); // White

  // Accent colors (used for buttons, highlights, etc.)
  static const Color accent = Color(0xff4caf50); // Light green
  static const Color accentAlternative = Color(0xff9c27b0); // Light purple

  // Text colors (used for body text, labels, etc.)
  static const Color textPrimary = Color(0xff000000); // Black
  static const Color textSecondary = Color(0xff757575); // Gray

  // Background colors (used for content areas, cards, etc.)
  static const Color background = Color(0xfff5f5f5); // Light gray
  static const Color backgroundLight = Color(0xffffffff); // White

  // Error color (used for error messages, warnings, etc.)
  static const Color error = Color(0xffff0000); // Red



  // You can add more colors here as needed
}

const Color darkCanvas = Color(0xFF111118);
const Color lightCanvas = Color(0xFFF8F8F8);
const Color darkCardColor = Color(0xFF1E1F2C);
const Color lightCardColor = Colors.white;
const Color primaryColor = Colors.deepPurple;

const Color whiteColor = Colors.white;
